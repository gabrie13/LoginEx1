@model LogInEx1.Models.PositionViewModel

@{
    ViewBag.Title = "Position | Delete";
}

<h3 class="alert alert-danger">Are you sure you want to delete this position?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PositionTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PositionTitle)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
